-r requirements.txt

# Testing
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-cov>=4.1.0
pytest-homeassistant-custom-component>=0.13.0
pytest-timeout>=2.1.0

# Mocking
freezegun>=1.2.0
# Used by the vendored client's own tests, moved here in s07.
aresponses>=3.0,<4.0
# Every runtime requirement the manifest declares must also be installed for the
# tests, or CI fails at import while a developer's venv (which still has leftovers
# from the pre-vendoring client install) passes. That is exactly how protobuf was
# missed: the vendored parallax.py imports google.protobuf at module scope.
# Keep this list in step with manifest.json's "requirements".
protobuf>=6.31,<6.33
# manifest.json declares bleak, so the test environment must have it too --
# without it the vendored BLE modules are 353 statements at 0% and their tests
# silently skip, which reads as coverage debt rather than an absent dependency.
bleak>=0.21

# Pulled in by homeassistant.components.bluetooth, which tests/test_button.py imports.
# Absent from HA's own metadata, so without these the bluetooth import fails and
# test_button.py errors at collection. Installed rather than mocked — the previous
# workaround was a module-level sys.modules stub that leaked across the whole session.
aiousbwatcher
pyserial

# Pinned because `pip install -r` does NOT apply HA's package_constraints.txt the way
# Home Assistant does at runtime, so an unconstrained resolve lands outside what HA
# can actually run:
#   pycares 5.x dropped ares_query_a_result, which aiodns==3.5.0 still calls
#   habluetooth 6.x needs a bleak API that bleak==1.0.1 does not expose
# Keep these aligned with homeassistant/package_constraints.txt when HA is bumped.
pycares<5
habluetooth==5.6.4
bleak-retry-connector==4.4.3
dbus-fast==2.44.3
