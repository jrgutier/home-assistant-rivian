#!/usr/bin/env bash
# f3a — retired. groups_for_model / VEHICLE_MODEL_GRANTS deleted in s26.
# Optional-hardware gating replaced the model map; R2 still gets ungated
# entities via the flat SENSORS/BINARY_SENSORS tuples.

source "$(dirname "$0")/_lib.sh"

echo "f3a retired: groups_for_model / VEHICLE_MODEL_GRANTS deleted in s26"
ok "f3a retired: groups_for_model / VEHICLE_MODEL_GRANTS deleted in s26"
summary f3a
