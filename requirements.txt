# Home Assistant
homeassistant>=2025.1.0
numpy
python-dateutil
PyTurboJPEG

# Integration
rivian-python-client[ble] @ git+https://github.com/jrgutier/rivian-python-client.git@1000a240999f747637daa43a3aa66660bf42f193

# Development
colorlog
pip>=21.0
pre-commit
# Deliberately NOT pinned, decided at the s05 review: the repo tracks upstream's
# .pre-commit-config rev instead. The newer rules that a pin used to hide were real
# findings (blind excepts, logging.exception, nested ifs) and are now fixed, so the
# tree is clean under the current release. The accepted cost is that a future ruff
# release can turn CI red on a day nobody changed any code.
ruff>=0.16.2
